# Local Claude harness config (not part of the public site)
.claude/

# Editor cruft
.DS_Store
*.swp
*.swo

# Python
__pycache__/
*.pyc
.venv/
venv/

# idacs.json is generated by scripts/build.py at deploy time. We commit it
# anyway so the repo is browseable as a static site without running the
# build locally; comment the next line out if you'd rather not.
# idacs.json
